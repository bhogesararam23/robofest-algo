# RoboFest simulation description

The package defines the 60 m by 15 m arena, exit-zone marker, a minimal drone URDF, and the `robofest_simulation.launch.py` wiring entry point. Gazebo and ArduPilot are launch-conditional so the same launch file can be used for offline node-graph tests.
